{
  "name": "db-manage",
  "version": "0.0.1",
  "description": "manage dynamodb tables with aws js sdk",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "marcus.bailey.dev",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.16.0",
    "@aws-sdk/lib-dynamodb": "^3.16.0"
  }
}
